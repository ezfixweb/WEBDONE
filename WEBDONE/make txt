
Logo od lask


command to admmin age showSupportDialog()


https://console.cloud.google.com/auth/clients?authuser=2&organizationId=0&project=famous-biplane-486722-e3


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



also add that when multicolor printer is selected you can select more colours it need to work like this you select the multi color printer and above the colors will apear selection for color 1 to 5 the apearing needs to be animated also i need to be able to change it in the catalog manager 






in![alt text](image.png)


https://client.packeta.com/en/support

c  turn on admin catalog
add česká pošta ppl dpd gls plus platba kartou 
info ceny 


test run 

DEPLOY (FREE, SQLITE, NO GOPAY)

Frontend (Netlify)
1) Create site on https://netlify.com
2) Base dir: root
3) Build command: (empty)
4) Publish dir: .
5) Deploy, get URL: https://your-site.netlify.app

Backend (Render)
1) Create web service on https://render.com
2) Root directory: backend
3) Build: npm install
4) Start: node server.js
5) Env vars:
	JWT_SECRET=your_secret
	JWT_EXPIRE=7d
	EMAIL_USER=ezfix.podpora@gmail.com
	EMAIL_PASSWORD=your_gmail_app_password
	FRONTEND_URL=https://your-site.netlify.app
	GOOGLE_CLIENT_ID=...
	GOOGLE_CLIENT_SECRET=...
	GOOGLE_REDIRECT_URI=https://your-backend.onrender.com/api/auth/google/callback
	ADMIN_EMAILS=your-email@example.com
	OWNER_USERNAME=your_username

IMPORTANT: SQLite can reset on free hosting (Render). OK for testing.

Update frontend API URL in script.js
Replace API_BASE_URL with your backend URL:
https://your-backend.onrender.com/api

CZ HOSTING FOR NODE API (CZECH DATA CENTER)
Option A: VPS in CZ (recommended)
- You need a VPS that allows Node.js + PM2 + Nginx.
- Any Czech VPS works (ask provider for Node.js support).

Basic VPS setup steps:
1) SSH to VPS
2) Install Node.js LTS + npm
3) Upload backend folder
4) npm install
5) Run with pm2: pm2 start server.js --name ezfix-api
6) Nginx reverse proxy to localhost:3000
7) Add SSL with certbot

Active24
- Domain + web hosting is OK for frontend
- For Node API you need VPS or dedicated server (not shared hosting)

If you want, tell me the provider name and I will write exact steps.